import { buttonTheme as Button } from "./button";

export const components = {
  Button,
  FormLabel: {
    baseStyle: {
      display: "flex",
      alignItems: "center",
      fontWeight: "normal",
      fontSize: "sm",
      gap: 2,
    },
  },
};
